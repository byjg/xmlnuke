<?php
/* *******************************************************************************************
 * Atention:
 * This PHP page enable the last processing in resulting XHTML before send back to the user
 * 
 * There is TWO variables pre-defined:
 *   $buffer - Contain the buffer XHTML
 *   $context - Containt the XMLNuke Context
 *
 */

// Code Example:

$buffer = str_replace("other cms", "xmlnuke", $buffer);
?>
